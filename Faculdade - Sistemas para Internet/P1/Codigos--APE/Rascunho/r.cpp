#include <bits/stdc++.h>

using namespace std;

int main() {
    string exp;
    while (getline(cin, exp)) {
        istringstream iss(exp);
        string parenteses;
        
        while (iss >> parenteses){
            if parenteses == '('
        }
    }

    return 0;
}
